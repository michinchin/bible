import 'package:flutter/material.dart';

class LearnVolumeDetail extends StatelessWidget {
  const LearnVolumeDetail({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
      body: const Center(child: Text('still working on detail view...')),
    );
  }
}
